<Window x:Class="Tester.CreateTestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tester"
        mc:Ignorable="d"
        Title="CreateTestView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Label Content="Название"/>
            <TextBox x:Name="testNameTextBox"/>
            <Label Content="Список вопросов"/>
            <ListBox Height="300">
                
            </ListBox>
            <Button x:Name="createQuestionButton" Content="Добавить вопрос"/>
            <Button x:Name="deleteSelectedQuestionButton" Content="Удалить вопрос"/>
        </StackPanel>
        <Grid>
            
        </Grid>
    </Grid>
</Window>
