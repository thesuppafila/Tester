<Window x:Class="Tester.TestCreator.CreateStudentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tester.TestCreator"
        mc:Ignorable="d"
        Title="CreateStudentView" Height="150" Width="250" SizeToContent="WidthAndHeight"
        FontSize="14"
        xmlns:xc="clr-namespace:Tester.ViewModel"
        xc:DialogCloser.DialogResult="{Binding DialogResult}">
    <Grid Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="ФИО студента" Grid.Row="0"/>
        <TextBox Grid.Row="1" Text="{Binding Name}"/>
        <Grid Grid.Row="2" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="100"/>
                <ColumnDefinition Width="*" MinWidth="100"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding CancelCommand}" Content="Отмена"/>
            <Button Grid.Column="1" Command="{Binding OkCommand}" Content="Добавить"/>
        </Grid>
    </Grid>
</Window>
